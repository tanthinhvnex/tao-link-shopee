name = "shopee-affiliate"
main = "worker.js"
compatibility_date = "2024-01-01"

# Uncomment dòng dưới nếu muốn dùng custom domain
# routes = [{ pattern = "api.yourdomain.com/*", zone_name = "yourdomain.com" }]
